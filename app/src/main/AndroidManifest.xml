<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hrobbie.recycleviewdemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".demo.BaseActivity" />
        <activity android:name=".demo.GridActivity" />
        <activity android:name=".demo.FourLevelActivity" />
        <activity android:name=".demo.RefreshActivity" />
        <activity android:name=".demo.StaggeredActivity" />
        <activity android:name=".demo.DifferentItemActivity"></activity>
    </application>

</manifest>